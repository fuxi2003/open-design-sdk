{
  "name": "@opendesign/api",
  "version": "0.1.3-rc.1",
  "main": "dist",
  "scripts": {
    "build": "yarn run clean-build && yarn run build:ts",
    "build:ts": "tsc --outDir dist --sourceMap --declaration --project ./tsconfig-build.json",
    "clean-build": "rm -rf dist"
  },
  "devDependencies": {
    "@opendesign/octopus-reader": "0.1.3-rc.1",
    "open-design-api-types": "0.3.0",
    "typescript": "4.1.2",
    "utility-types": "3.10.0"
  },
  "dependencies": {
    "cplus": "2.0.1",
    "form-data": "4.0.0",
    "node-fetch": "3.0.0-beta.9"
  }
}
