{
  "private": true,
  "version": "0.0.0",
  "workspaces": {
    "packages": [
    ],
    "nohoist": [
      "*"
    ]
  },
  "scripts": {
    "lint": "yarn run lint:prettier",
    "lint:prettier": "yarn run prettier -c ."
  },
  "devDependencies": {
    "@types/node": "*",
    "prettier": "2.2.1",
    "typescript": "4.1.2"
  }
}
